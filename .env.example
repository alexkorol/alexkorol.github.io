# Public Worker endpoint; safe to expose in the browser bundle.
VITE_PORTFOLIO_API_URL=https://portfolio-rag.example.workers.dev

# Worker secrets are set with `wrangler secret put` and never VITE_-prefixed.
# ANTHROPIC_API_KEY=
